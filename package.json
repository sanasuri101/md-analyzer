{
  "name": "md-analyzer",
  "version": "1.0.0",
  "description": "CLI tool that analyzes Markdown files for word count, reading time, and heading structure",
  "type": "module",
  "bin": {
    "md-analyzer": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "start": "node dist/cli.js"
  },
  "keywords": ["markdown", "analyzer", "cli", "word-count", "reading-time"],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
